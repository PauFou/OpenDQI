# OpenDQI mapping for the synthetic internal-book CSV.
#
# Keys are canonical EMIR record field names; values are the header
# names found in the book CSV. Only the fields needed for
# `book-reconcile` cross-source comparison are listed.

fields:
  uti: trade_uti
  notional_amount: notional
  notional_currency: notional_ccy
  maturity_date: maturity
  valuation_amount: valuation
  valuation_currency: valuation_ccy
  termination_date: terminated_on

date_format: "%Y-%m-%d"
datetime_format: "%Y-%m-%dT%H:%M:%S%.fZ"
