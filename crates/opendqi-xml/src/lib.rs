//! OpenDQI XML ingestion and XSD validation.
//!
//! Reserved module. XML well-formedness, EMIR/SFTR extraction, and XSD
//! validation adapters arrive in milestones 0.2 and 0.3.

#![forbid(unsafe_code)]

/// Placeholder so this crate compiles as part of the workspace.
pub fn placeholder() -> &'static str {
    "opendqi-xml: implementation pending"
}
